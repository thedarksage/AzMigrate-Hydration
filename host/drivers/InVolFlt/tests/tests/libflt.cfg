rundir_base=${rundir_base:-"/var/run/wal"}
rundir=${rundir:-"$rundir_base/$pname"}
logdir=${logdir:-"/var/log/wal/$pname"}
logfile=${logfile:-"$logdir/$pname.log"}

# Single exec
initdir=${initdir:-"$rundir_base/init"}

asrpath=${asrpath:-"/usr/local/ASR/Vx"}
