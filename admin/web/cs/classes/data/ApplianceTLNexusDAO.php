<?php
/**
  * This sourcecode has been generated by InMage Source Code Generator version 1.0
  * The usage of generated code is restricted to InMage Systems Inc.
  *
  * applianceTLNexus Data Access Object[DAO]
  * This class contains all database handling that is needed to 
  * permanently store and retrieve applianceTLNexus object instances. 
  * Generated Date:	Tue, 15 Apr 2008 20:16:58 +0530	
  * Pattern Version:1.0
  */
class ApplianceTLNexusDAO
{

/**
 * createValueObject-method. This method is used when the Dao class needs
 * to create new value object instance. The reason why this method exists
 * is that sometimes the programmer may want to extend also the valueObject
 * and then this method can be overrided to return extended valueObject.
 * NOTE: If you extend the valueObject class, make sure to override the
 * clone() method in it!
 */
function createValueObject() {
	return new ApplianceTLNexusVO();
 }

/**
 * getObject-method. This will create and load valueObject contents from database 
 * using given Primary-Key as identifier. This method is just a convenience method 
 * for the real load-method which accepts the valueObject as a parameter. Returned
 * valueObject will be created using the createValueObject() method.
 */
function getObject($conn,$atLunNumber,$atPortWwn,$fabricId,$hostId,$sanLunId) {
	$valueObject = $this->createValueObject();
	$valueObject->set_at_lun_number($atLunNumber);
	$valueObject->set_at_port_wwn($atPortWwn);
	$valueObject->set_fabric_id($fabricId);
	$valueObject->set_host_id($hostId);
	$valueObject->set_san_lun_id($sanLunId);
	$this->findByPrimaryKey($conn,$valueObject);
	return $valueObject;
}

/**
 * findbyPrimaryKey-method. This will load valueObject contents from database using
 * Primary-Key as identifier. Upper layer should use this so that valueObject
 * instance is created and only primary-key should be specified. Then call
 * this method to complete other persistent information. This method will
 * overwrite all other fields except primary-key and possible runtime variables.
 * If load can not find matching row, NotFoundException will be thrown.
 *
 * @param conn         This method requires working database connection.
 * @param valueObject  This parameter contains the class instance to be loaded.
 *                     Primary-key field must be set for this to work properly.
 */
function findByPrimaryKey($conn, $valueObject) {

	if (!$valueObject->get_at_lun_number()) {
    	//print "Can not select without Primary-Key!";
		return false;
	}

	if (!$valueObject->get_at_port_wwn()) {
    	//print "Can not select without Primary-Key!";
		return false;
	}

	if (!$valueObject->get_fabric_id()) {
    	//print "Can not select without Primary-Key!";
		return false;
	}

	if (!$valueObject->get_host_id()) {
    	//print "Can not select without Primary-Key!";
		return false;
	}

	if (!$valueObject->get_san_lun_id()) {
    	//print "Can not select without Primary-Key!";
		return false;
	}

	$sql = "SELECT * FROM applianceTLNexus WHERE (atLunNumber = '".$valueObject->get_at_lun_number()."'  AND atPortWwn = '".$valueObject->get_at_port_wwn()."'  AND fabricId = '".$valueObject->get_fabric_id()."'  AND hostId = '".$valueObject->get_host_id()."'  AND sanLunId = '".$valueObject->get_san_lun_id()."') "; 
	if ($this->singleQuery($conn, $sql, $valueObject)){
    	return true;
    }
    else{
    	return false;
	}
}

/**
 * findbyfabric_id-method. A user defined or custom findbyMethod  
 * This method select by fabric_id
 *
 * @param conn         This method requires working database connection.
 * @param params       associate array: sql will be searched and replaced by the matching values
 *					   associated with the token
 */
function find_by_fabric_id($conn, $params = array()) {

	$sql = "fabricId = 'fabric_id'";
	$sql = str_replace(array_keys($params), array_values($params), $sql);
	$sql = "SELECT * FROM applianceTLNexus WHERE (" . $sql . ")";
	$searchResults = $this->listQuery($conn, $sql);
	return $searchResults;
		
}

/**
 * findAll-method. This will read all contents from database table and
 * build an Vector containing valueObjects. Please note, that this method
 * will consume huge amounts of resources if table has lot's of rows. 
 * This should only be used when target tables have only small amounts
 * of data.
 *
 * @param conn         This method requires working database connection.
 */
function findAll($conn) {
	
	$sql = "SELECT * FROM applianceTLNexus ORDER BY atLunNumber ASC ,atPortWwn ASC ,fabricId ASC ,hostId ASC ,sanLunId ASC ";
	$searchResults = $this->listQuery($conn, $sql);
	return $searchResults;
}

/**
 * create-method. This will create new row in database according to supplied
 * valueObject contents. Make sure that values for all NOT NULL columns are
 * correctly specified. Also, if this table does not use automatic surrogate-keys
 * the primary-key must be specified. After INSERT command this method will 
 * read the generated primary-key back to valueObject if automatic surrogate-keys
 * were used. 
 *
 * @param conn         This method requires working database connection.
 * @param valueObject  This parameter contains the class instance to be created.
 *                     If automatic surrogate-keys are not used the Primary-key 
 *                     field must be set for this to work properly.
 */
function create($conn, $valueObject) {
	 $count = 0;
     $sql1 = "INSERT INTO applianceTLNexus(";
     $sql3 = ") VALUES ("; 
	 
	 if ($valueObject->isset_at_port_wwn())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."atPortWwn";
 	    $sql4 = $sql4."'".$valueObject->get_at_port_wwn()."'"; 	
 		$count++;
 	 }
	 
	 if ($valueObject->isset_host_id())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."hostId";
 	    $sql4 = $sql4."'".$valueObject->get_host_id()."'"; 	
 		$count++;
 	 }
	 
	 if ($valueObject->isset_fabric_id())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."fabricId";
 	    $sql4 = $sql4."'".$valueObject->get_fabric_id()."'"; 	
 		$count++;
 	 }
	 
	 if ($valueObject->isset_san_lun_id())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."sanLunId";
 	    $sql4 = $sql4."'".$valueObject->get_san_lun_id()."'"; 	
 		$count++;
 	 }
	 
	 if ($valueObject->isset_at_lun_number())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."atLunNumber";
 	    $sql4 = $sql4."'".$valueObject->get_at_lun_number()."'"; 	
 		$count++;
 	 }
	 
	$sql = $sql1 . $sql2. $sql3. $sql4.")";
	$result = $this->databaseUpdate($conn, $sql);
	return true;
}

/**
 * update-method. This method will save the current state of valueObject to database.
 * Save can not be used to create new instances in database, so upper layer must
 * make sure that the primary-key is correctly specified. Primary-key will indicate
 * which instance is going to be updated in database. If save can not find matching 
 * row, NotFoundException will be thrown.
 *
 * @param conn         This method requires working database connection.
 * @param valueObject  This parameter contains the class instance to be saved.
 *                     Primary-key field must be set for this to work properly.
 */
function update($conn, $valueObject) {
	$count = 0;
	$sql = "UPDATE applianceTLNexus SET ";
	if ($valueObject->isset_at_port_wwn())
 	{
		if($count>0)
		{
			$sql = $sql . ",";
		}
		$sql = $sql."atPortWwn = '".$valueObject->get_at_port_wwn()."'";
		$count++;
	}
	
	if ($valueObject->isset_host_id())
 	{
		if($count>0)
		{
			$sql = $sql . ",";
		}
		$sql = $sql."hostId = '".$valueObject->get_host_id()."'";
		$count++;
	}
	
	if ($valueObject->isset_fabric_id())
 	{
		if($count>0)
		{
			$sql = $sql . ",";
		}
		$sql = $sql."fabricId = '".$valueObject->get_fabric_id()."'";
		$count++;
	}
	
	if ($valueObject->isset_san_lun_id())
 	{
		if($count>0)
		{
			$sql = $sql . ",";
		}
		$sql = $sql."sanLunId = '".$valueObject->get_san_lun_id()."'";
		$count++;
	}
	
	if ($valueObject->isset_at_lun_number())
 	{
		if($count>0)
		{
			$sql = $sql . ",";
		}
		$sql = $sql."atLunNumber = '".$valueObject->get_at_lun_number()."'";
		$count++;
	}
	
	$sql = $sql." WHERE (atLunNumber = '".$valueObject->get_at_lun_number()."'  AND atPortWwn = '".$valueObject->get_at_port_wwn()."'  AND fabricId = '".$valueObject->get_fabric_id()."'  AND hostId = '".$valueObject->get_host_id()."'  AND sanLunId = '".$valueObject->get_san_lun_id()."')"; 
	$result = $this->databaseUpdate($conn, $sql);
    if ($result != 1) {
        //print "PrimaryKey Error when updating DB!";
    	return false;
	}
 	return true;
}

/**
 * delete-method. This method will remove the information from database as identified by
 * by primary-key in supplied valueObject. Once valueObject has been deleted it can not 
 * be restored by calling save. Restoring can only be done using create method but if 
 * database is using automatic surrogate-keys, the resulting object will have different 
 * primary-key than what it was in the deleted object. If delete can not find matching row,
 * NotFoundException will be thrown.
 *
 * @param conn         This method requires working database connection.
 * @param valueObject  This parameter contains the class instance to be deleted.
 *                     Primary-key field must be set for this to work properly.
 */
function delete($conn, $valueObject) {
	
	if (!$valueObject->get_at_lun_number()) {
         //print "Can not delete without Primary-Key!";
         return false;
    }
	
	if (!$valueObject->get_at_port_wwn()) {
         //print "Can not delete without Primary-Key!";
         return false;
    }
	
	if (!$valueObject->get_fabric_id()) {
         //print "Can not delete without Primary-Key!";
         return false;
    }
	
	if (!$valueObject->get_host_id()) {
         //print "Can not delete without Primary-Key!";
         return false;
    }
	
	if (!$valueObject->get_san_lun_id()) {
         //print "Can not delete without Primary-Key!";
         return false;
    }
	$sql = "DELETE FROM applianceTLNexus WHERE (atLunNumber = '".$valueObject->get_at_lun_number()."'  AND atPortWwn = '".$valueObject->get_at_port_wwn()."'  AND fabricId = '".$valueObject->get_fabric_id()."'  AND hostId = '".$valueObject->get_host_id()."'  AND sanLunId = '".$valueObject->get_san_lun_id()."')"; 
	$result = $this->databaseUpdate($conn, $sql);
    if ($result != 1) {
         //print "PrimaryKey Error when updating DB!";
          return false;
    }
 	return true;
}

/**
 * deleteAll-method. This method will remove all information from the table that matches
 * this DAO and ValueObject couple. This should be the most efficient way to clear table.
 * Once deleteAll has been called, no valueObject that has been created before can be 
 * restored by calling save. Restoring can only be done using create method but if database 
 * is using automatic surrogate-keys, the resulting object will have different primary-key 
 * than what it was in the deleted object. (Note, the implementation of this method should
 * be different with different DB backends.)
 *
 * @param conn         This method requires working database connection.
 */
function deleteAll($conn) {
	$sql = "DELETE FROM applianceTLNexus";
	$result = $this->databaseUpdate($conn, $sql);
	return true;
}

/**
 * coutAll-method. This method will return the number of all rows from table that matches
 * this Dao. The implementation will simply execute "select count(primarykey) from table".
 * If table is empty, the return value is 0. This method should be used before calling
 * findAll, to make sure table has not too many rows.
 *
 * @param conn         This method requires working database connection.
 */
function countAll($conn) {
	$sql = "SELECT count(*) FROM applianceTLNexus";
	$allRows = 0;
	$result = $conn->execute($sql);
    if ($row = $conn->nextRow($result))
    	$allRows = $row[0];
    return $allRows;
}

/** 
 * searchMatching-Method. This method provides searching capability to 
 * get matching valueObjects from database. It works by searching all 
 * objects that match permanent instance variables of given object.
 * Upper layer should use this by setting some parameters in valueObject
 * and then  call searchMatching. The result will be 0-N objects in vector, 
 * all matching those criteria you specified. Those instance-variables that
 * have NULL values are excluded in search-criteria.
 *
 * @param conn         This method requires working database connection.
 * @param valueObject  This parameter contains the class instance where search will be based.
 *                     Primary-key field should not be set.
 */
function searchMatching($conn, $valueObject) {
	$first = true;
    $sql = "SELECT * FROM applianceTLNexus WHERE 1=1 ";
	
	if ($valueObject->get_at_port_wwn() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND atPortWwn LIKE '".$valueObject->get_at_port_wwn()."%' ";
    }
	
	if ($valueObject->get_host_id() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND hostId LIKE '".$valueObject->get_host_id()."%' ";
    }
	
	if ($valueObject->get_fabric_id() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND fabricId LIKE '".$valueObject->get_fabric_id()."%' ";
    }
	
	if ($valueObject->get_san_lun_id() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND sanLunId LIKE '".$valueObject->get_san_lun_id()."%' ";
    }
	
	if ($valueObject->get_at_lun_number() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND atLunNumber LIKE '".$valueObject->get_at_lun_number()."%' ";
    }
	
	$sql = $sql." ORDER BY ";
	$sql = $sql."atLunNumber ASC ";
    $sql = $sql .", ";
	$sql = $sql."atPortWwn ASC ";
    $sql = $sql .", ";
	$sql = $sql."fabricId ASC ";
    $sql = $sql .", ";
	$sql = $sql."hostId ASC ";
    $sql = $sql .", ";
	$sql = $sql."sanLunId ASC ";
    // Prevent accidential full table results.
    // Use findAll if all rows must be returned.
    if ($first)
    	return array();

    $searchResults = $this->listQuery($conn, $sql);
    return $searchResults;
}

/**
 * databaseQuery-method. This method is a helper method for internal use. It will execute
 * all database queries that will return only one row. The resultset will be converted
 * to valueObject. If no rows were found, NotFoundException will be thrown.
 *
 * @param conn         This method requires working database connection.
 * @param stmt         This parameter contains the SQL statement to be excuted.
 * @param valueObject  Class-instance where resulting data will be stored.
 */
 function singleQuery($conn, $sql, $valueObject) {
 	$result = $conn->execute($sql);
	if ($row = $conn->nextRow($result)) {
		$valueObject->setAry($row);
     } else {
        //print " Object Not Found!";
     	return false;
     }
     return true;
 }

/**
 * databaseQuery-method. This method is a helper method for internal use. It will execute
 * all database queries that will return multiple rows. The resultset will be converted
 * to the List of valueObjects. If no rows were found, an empty List will be returned.
 *
 * @param conn         This method requires working database connection.
 * @param stmt         This parameter contains the SQL statement to be excuted.
*/
function listQuery($conn, $sql) {
	$searchResults = array();
    $result = $conn->execute($sql);
    while ($row = $conn->nextRow($result)) {
    	$temp = $this->createValueObject();
    	$temp->setAry($row);
        array_push($searchResults, $temp);
	}
	return $searchResults;
}
/**
 * databaseQuery-method. This method is a helper method for internal use. It will execute
 * all database queries that will return multiple rows. However the results will be unstructured
 * i.e. Not formatted as VO Objects 
 * @param conn         This method requires working database connection.
 * @param stmt         This parameter contains the SQL statement to be excuted.
*/
function listQueryResultset($conn,$sql)
{
	$searchResults = array();
    $result = $conn->execute($sql);
	while ($row = $conn->nextRow($result)) {
        array_push($searchResults, $row);
	}
	return $searchResults;
}

/**
 * databaseUpdate-method. This method is a helper method for internal use. It will execute
 * all database handling that will change the information in tables. SELECT queries will
 * not be executed here however. The return value indicates how many rows were affected.
 * This method will also make sure that if cache is used, it will reset when data changes.
 *
 * @param conn         This method requires working database connection.
 * @param stmt         This parameter contains the SQL statement to be excuted.
 */
function databaseUpdate($conn, $sql) {
	$result = $conn->execute($sql);
	return $result;
}

/** 
 * getCodegenVersion will return information about
 * generator which created these sources.
 */
function get_codegen_version() {
    return  "1.0";
 }
/** 
 * getCodegenTemplateVersion will return information about
 * generator which created these sources.
 */
function get_codegen_template_version() {
    return  "PHPDAO-1.10";
 }

}
?>
