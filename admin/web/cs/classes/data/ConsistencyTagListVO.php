<?php
/**
 * This sourcecode has been generated by InMage Source Code Generator version 1.0
 * The usage of generated code is restricted to InMage Systems Inc.
 *
 * consistencyTagList Value Object[VO]
 * This class is value object representing database table host
 * This class is needs to be used with associated Dao object
 * Generated Date:	Tue, 27 Nov 2007 14:01:23 +0530	
 * Pattern Version:1.0
 */

class ConsistencyTagListVO
{

/**
 * Global variables 
 */
 
 public $ary;
 
 
 
/** 
 * Persistent Instance variables. This data is directly 
 * mapped to the columns of database table.
 */

public $id;
public $ValueName;
public $ValueData;
public $ValueType;

/** 
 * Constructors. Code generator generates two constructors by default.
 * The first one takes no arguments and provides the most simple
 * way to create object instance. The another one takes one
 * argument, which is the primary key of the corresponding table.
 */

function ConsistencyTagListVO(){

}

/*
	function ConsistencyTagListVO(idIn){
 this->id=$idIn;
}
*/

/** 
 * Initializes the variables with database specific 
 * defaults.
 */
 function initialize(){
 
 }
 
/** 
 * Get and Set methods for persistent variables. 
 */

function get_id() {
	return $this->id;
}
function set_id($idIn) { 
	$this->id=$idIn;
}

function isset_id() {
	return isset($this->id);
}


function get_value_name() {
	return $this->ValueName;
}
function set_value_name($ValueNameIn) { 
	$this->ValueName=$ValueNameIn;
}

function isset_value_name() {
	return isset($this->ValueName);
}


function get_value_data() {
	return $this->ValueData;
}
function set_value_data($ValueDataIn) { 
	$this->ValueData=$ValueDataIn;
}

function isset_value_data() {
	return isset($this->ValueData);
}


function get_value_type() {
	return $this->ValueType;
}
function set_value_type($ValueTypeIn) { 
	$this->ValueType=$ValueTypeIn;
}

function isset_value_type() {
	return isset($this->ValueType);
}


/** 
 * setAll allows to set all persistent variables in one method call.
 * This is useful, when all data is available and it is needed to 
 * set the initial state of this object. Note that this method will
 * directly modify instance variales, without going trough the 
 * individual set-methods.
 */
function setAll($idIn,$ValueNameIn,$ValueDataIn,$ValueTypeIn){
	$this->id=$idIn;
	$this->ValueName=$ValueNameIn;
	$this->ValueData=$ValueDataIn;
	$this->ValueType=$ValueTypeIn;
}

/** 
 * setAry allows copy the contents of an associative array 
 * to the member variables
 */
function setAry($ary){ 
	$this->ary=$ary;
	$this->id=$ary["id"];
	$this->ValueName=$ary["ValueName"];
	$this->ValueData=$ary["ValueData"];
	$this->ValueType=$ary["ValueType"];
}

/** 
 * returns an associative array representing the object
 * if the object values are loaded as result of DB operation, then the array
 * return would equivalent of the row returned DB resultset 
 */
function getAry(){ 
	$oAry = array();
	if (isset($this->ary))
	{
		$oAry = $this->ary;
	}
	else
	{
		$oAry["id"]=$this->id;
		$oAry["ValueName"]=$this->ValueName;
		$oAry["ValueData"]=$this->ValueData;
		$oAry["ValueType"]=$this->ValueType;
	}
	return $oAry;
} 

 /** 
  * hasEqualMapping-method will compare two Host instances
  * and return true if they contain same values in all persistent instance 
  * variables. If hasEqualMapping returns true, it does not mean the objects
  * are the same instance. However it does mean that in that moment, they 
  * are mapped to the same row in database.
  */
function hasEqualMapping($valueObject){
	if ($valueObject->get_id() != $this->id) {
		return(false);
	}
	if ($valueObject->get_value_name() != $this->ValueName) {
		return(false);
	}
	if ($valueObject->get_value_data() != $this->ValueData) {
		return(false);
	}
	if ($valueObject->get_value_type() != $this->ValueType) {
		return(false);
	}
    return true;
 }
 
/**
 * toString will return String object representing the state of this 
 * valueObject. This is useful during application development, and 
 * possibly when application is writing object states in textlog.
 */
function toString() {
	$out = $this->get_codegen_version();
	$out = $out."\nclass ConsistencyTagListVO, mapping to table consistencyTagList\n";
	$out = $out."Persistent attributes: \n";
	$out = $out."id = ".$this->id."\n";
	$out = $out."ValueName = ".$this->ValueName."\n";
	$out = $out."ValueData = ".$this->ValueData."\n";
	$out = $out."ValueType = ".$this->ValueType."\n";
	return $out;
}

/**
 * Clone will return identical deep copy of this valueObject.
 * Here, the retuned cloned object will have all its attributes cloned
 */
function __clone() {
	$cloned = new ConsistencyTagListVO();
	
	 $cloned->set_id($this->id);
	 $cloned->set_value_name($this->ValueName);
	 $cloned->set_value_data($this->ValueData);
	 $cloned->set_value_type($this->ValueType);
	
	return $cloned;
}

/** 
 * getCodegenVersion will return information about
 * generator which created these sources.
 */
function get_codegen_version() {
    return  "1.0";
 }
/** 
 * getCodegenTemplateVersion will return information about
 * generator which created these sources.
 */
function get_codegen_template_version() {
    return  "PHPVO-1.7";
 }

}
?>
