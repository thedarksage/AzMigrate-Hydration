<?php
/**
  * This sourcecode has been generated by InMage Source Code Generator version 1.0
  * The usage of generated code is restricted to InMage Systems Inc.
  *
  * clusters Data Access Object[DAO]
  * This class contains all database handling that is needed to 
  * permanently store and retrieve clusters object instances. 
  * Generated Date:	Tue, 27 Nov 2007 14:01:23 +0530	
  * Pattern Version:1.0
  */
class ClustersDAO
{

/**
 * createValueObject-method. This method is used when the Dao class needs
 * to create new value object instance. The reason why this method exists
 * is that sometimes the programmer may want to extend also the valueObject
 * and then this method can be overrided to return extended valueObject.
 * NOTE: If you extend the valueObject class, make sure to override the
 * clone() method in it!
 */
function createValueObject() {
	return new ClustersVO();
 }

/**
 * getObject-method. This will create and load valueObject contents from database 
 * using given Primary-Key as identifier. This method is just a convenience method 
 * for the real load-method which accepts the valueObject as a parameter. Returned
 * valueObject will be created using the createValueObject() method.
 */
function getObject($conn) {
	$valueObject = $this->createValueObject();
	$this->findByPrimaryKey($conn,$valueObject);
	return $valueObject;
}

/**
 * findAll-method. This will read all contents from database table and
 * build an Vector containing valueObjects. Please note, that this method
 * will consume huge amounts of resources if table has lot's of rows. 
 * This should only be used when target tables have only small amounts
 * of data.
 *
 * @param conn         This method requires working database connection.
 */
function findAll($conn) {
	
	$sql = "SELECT * FROM clusters";
	$searchResults = $this->listQuery($conn, $sql);
	return $searchResults;
}

/**
 * create-method. This will create new row in database according to supplied
 * valueObject contents. Make sure that values for all NOT NULL columns are
 * correctly specified. Also, if this table does not use automatic surrogate-keys
 * the primary-key must be specified. After INSERT command this method will 
 * read the generated primary-key back to valueObject if automatic surrogate-keys
 * were used. 
 *
 * @param conn         This method requires working database connection.
 * @param valueObject  This parameter contains the class instance to be created.
 *                     If automatic surrogate-keys are not used the Primary-key 
 *                     field must be set for this to work properly.
 */
function create($conn, $valueObject) {
	 $count = 0;
     $sql1 = "INSERT INTO clusters(";
     $sql3 = ") VALUES ("; 
	 
	 if ($valueObject->isset_host_id())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."hostId";
 	    $sql4 = $sql4."'".$valueObject->get_host_id()."'"; 	
 		$count++;
 	 }
	 
	 if ($valueObject->isset_device_name())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."deviceName";
 	    $sql4 = $sql4."'".$valueObject->get_device_name()."'"; 	
 		$count++;
 	 }
	 
	 if ($valueObject->isset_device_id())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."deviceId";
 	    $sql4 = $sql4."'".$valueObject->get_device_id()."'"; 	
 		$count++;
 	 }
	 
	 if ($valueObject->isset_cluster_name())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."clusterName";
 	    $sql4 = $sql4."'".$valueObject->get_cluster_name()."'"; 	
 		$count++;
 	 }
	 
	 if ($valueObject->isset_cluster_group_name())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."clusterGroupName";
 	    $sql4 = $sql4."'".$valueObject->get_cluster_group_name()."'"; 	
 		$count++;
 	 }
	 
	 if ($valueObject->isset_cluster_group_id())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."clusterGroupId";
 	    $sql4 = $sql4."'".$valueObject->get_cluster_group_id()."'"; 	
 		$count++;
 	 }
	 
	 if ($valueObject->isset_clusterrule_id())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."clusterruleId";
 	    $sql4 = $sql4."'".$valueObject->get_clusterrule_id()."'"; 	
 		$count++;
 	 }
	 
	 if ($valueObject->isset_exchange_virtual_server_name())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."exchangeVirtualServerName";
 	    $sql4 = $sql4."'".$valueObject->get_exchange_virtual_server_name()."'"; 	
 		$count++;
 	 }
	 
	 if ($valueObject->isset_sql_virtual_server_name())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."sqlVirtualServerName";
 	    $sql4 = $sql4."'".$valueObject->get_sql_virtual_server_name()."'"; 	
 		$count++;
 	 }
	 
	 if ($valueObject->isset_file_server_virtual_server_name())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."fileServerVirtualServerName";
 	    $sql4 = $sql4."'".$valueObject->get_file_server_virtual_server_name()."'"; 	
 		$count++;
 	 }
	 
	 if ($valueObject->isset_cluster_resource_name())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."clusterResourceName";
 	    $sql4 = $sql4."'".$valueObject->get_cluster_resource_name()."'"; 	
 		$count++;
 	 }
	 
	 if ($valueObject->isset_cluster_resource_id())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."clusterResourceId";
 	    $sql4 = $sql4."'".$valueObject->get_cluster_resource_id()."'"; 	
 		$count++;
 	 }
	 
	 if ($valueObject->isset_cluster_id())
 	 {
 	  	if($count>0)
		{
			$sql2 = $sql2 . ",";
			$sql4 = $sql4 . ",";
		}
 	    $sql2 = $sql2."clusterId";
 	    $sql4 = $sql4."'".$valueObject->get_cluster_id()."'"; 	
 		$count++;
 	 }
	 
	$sql = $sql1 . $sql2. $sql3. $sql4.")";
	$result = $this->databaseUpdate($conn, $sql);
	return true;
}

/**
 * deleteAll-method. This method will remove all information from the table that matches
 * this DAO and ValueObject couple. This should be the most efficient way to clear table.
 * Once deleteAll has been called, no valueObject that has been created before can be 
 * restored by calling save. Restoring can only be done using create method but if database 
 * is using automatic surrogate-keys, the resulting object will have different primary-key 
 * than what it was in the deleted object. (Note, the implementation of this method should
 * be different with different DB backends.)
 *
 * @param conn         This method requires working database connection.
 */
function deleteAll($conn) {
	$sql = "DELETE FROM clusters";
	$result = $this->databaseUpdate($conn, $sql);
	return true;
}

/**
 * coutAll-method. This method will return the number of all rows from table that matches
 * this Dao. The implementation will simply execute "select count(primarykey) from table".
 * If table is empty, the return value is 0. This method should be used before calling
 * findAll, to make sure table has not too many rows.
 *
 * @param conn         This method requires working database connection.
 */
function countAll($conn) {
	$sql = "SELECT count(*) FROM clusters";
	$allRows = 0;
	$result = $conn->execute($sql);
    if ($row = $conn->nextRow($result))
    	$allRows = $row[0];
    return $allRows;
}

/** 
 * searchMatching-Method. This method provides searching capability to 
 * get matching valueObjects from database. It works by searching all 
 * objects that match permanent instance variables of given object.
 * Upper layer should use this by setting some parameters in valueObject
 * and then  call searchMatching. The result will be 0-N objects in vector, 
 * all matching those criteria you specified. Those instance-variables that
 * have NULL values are excluded in search-criteria.
 *
 * @param conn         This method requires working database connection.
 * @param valueObject  This parameter contains the class instance where search will be based.
 *                     Primary-key field should not be set.
 */
function searchMatching($conn, $valueObject) {
	$first = true;
    $sql = "SELECT * FROM clusters WHERE 1=1 ";
	
	if ($valueObject->get_host_id() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND hostId LIKE '".$valueObject->get_host_id()."%' ";
    }
	
	if ($valueObject->get_device_name() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND deviceName LIKE '".$valueObject->get_device_name()."%' ";
    }
	
	if ($valueObject->get_device_id() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND deviceId LIKE '".$valueObject->get_device_id()."%' ";
    }
	
	if ($valueObject->get_cluster_name() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND clusterName LIKE '".$valueObject->get_cluster_name()."%' ";
    }
	
	if ($valueObject->get_cluster_group_name() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND clusterGroupName LIKE '".$valueObject->get_cluster_group_name()."%' ";
    }
	
	if ($valueObject->get_cluster_group_id() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND clusterGroupId LIKE '".$valueObject->get_cluster_group_id()."%' ";
    }
	
	if ($valueObject->get_clusterrule_id() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND clusterruleId LIKE '".$valueObject->get_clusterrule_id()."%' ";
    }
	
	if ($valueObject->get_exchange_virtual_server_name() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND exchangeVirtualServerName LIKE '".$valueObject->get_exchange_virtual_server_name()."%' ";
    }
	
	if ($valueObject->get_sql_virtual_server_name() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND sqlVirtualServerName LIKE '".$valueObject->get_sql_virtual_server_name()."%' ";
    }
	
	if ($valueObject->get_file_server_virtual_server_name() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND fileServerVirtualServerName LIKE '".$valueObject->get_file_server_virtual_server_name()."%' ";
    }
	
	if ($valueObject->get_cluster_resource_name() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND clusterResourceName LIKE '".$valueObject->get_cluster_resource_name()."%' ";
    }
	
	if ($valueObject->get_cluster_resource_id() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND clusterResourceId LIKE '".$valueObject->get_cluster_resource_id()."%' ";
    }
	
	if ($valueObject->get_cluster_id() != "") {
    	if ($first) { $first = false; }
        $sql = $sql."AND clusterId LIKE '".$valueObject->get_cluster_id()."%' ";
    }
	
    // Prevent accidential full table results.
    // Use findAll if all rows must be returned.
    if ($first)
    	return array();

    $searchResults = $this->listQuery($conn, $sql);
    return $searchResults;
}

/**
 * databaseQuery-method. This method is a helper method for internal use. It will execute
 * all database queries that will return only one row. The resultset will be converted
 * to valueObject. If no rows were found, NotFoundException will be thrown.
 *
 * @param conn         This method requires working database connection.
 * @param stmt         This parameter contains the SQL statement to be excuted.
 * @param valueObject  Class-instance where resulting data will be stored.
 */
 function singleQuery($conn, $sql, $valueObject) {
 	$result = $conn->execute($sql);
	if ($row = $conn->nextRow($result)) {
		$valueObject->setAry($row);
     } else {
        //print " Object Not Found!";
     	return false;
     }
     return true;
 }

/**
 * databaseQuery-method. This method is a helper method for internal use. It will execute
 * all database queries that will return multiple rows. The resultset will be converted
 * to the List of valueObjects. If no rows were found, an empty List will be returned.
 *
 * @param conn         This method requires working database connection.
 * @param stmt         This parameter contains the SQL statement to be excuted.
*/
function listQuery($conn, $sql) {
	$searchResults = array();
    $result = $conn->execute($sql);
    while ($row = $conn->nextRow($result)) {
    	$temp = $this->createValueObject();
    	$temp->setAry($row);
        array_push($searchResults, $temp);
	}
	return $searchResults;
}
/**
 * databaseQuery-method. This method is a helper method for internal use. It will execute
 * all database queries that will return multiple rows. However the results will be unstructured
 * i.e. Not formatted as VO Objects 
 * @param conn         This method requires working database connection.
 * @param stmt         This parameter contains the SQL statement to be excuted.
*/
function listQueryResultset($conn,$sql)
{
	$searchResults = array();
    $result = $conn->execute($sql);
	while ($row = $conn->nextRow($result)) {
        array_push($searchResults, $row);
	}
	return $searchResults;
}

/**
 * databaseUpdate-method. This method is a helper method for internal use. It will execute
 * all database handling that will change the information in tables. SELECT queries will
 * not be executed here however. The return value indicates how many rows were affected.
 * This method will also make sure that if cache is used, it will reset when data changes.
 *
 * @param conn         This method requires working database connection.
 * @param stmt         This parameter contains the SQL statement to be excuted.
 */
function databaseUpdate($conn, $sql) {
	$result = $conn->execute($sql);
	return $result;
}

/** 
 * getCodegenVersion will return information about
 * generator which created these sources.
 */
function get_codegen_version() {
    return  "1.0";
 }
/** 
 * getCodegenTemplateVersion will return information about
 * generator which created these sources.
 */
function get_codegen_template_version() {
    return  "PHPDAO-1.9";
 }

}
?>
